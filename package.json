{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout 10000",
    "init-project": "rm -rf node_modules && rm -rf environments && rm -rf test && rm -rf models && rm -rf geocode && rm -rf weather && npm install && tsc && mocha --timeout 10000",
    "test-weather": "node app -a='1301 lambard street philadelphia'",
    "build": "tsc --listEmittedFiles"
  },
  "author": "Thiago Lima",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.13.1",
    "chai": "^4.2.0",
    "chai-http": "^4.2.0",
    "mocha": "^5.2.0",
    "request": "^2.73.0",
    "yargs": "^4.8.1"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/chai": "^4.1.7",
    "@types/chai-http": "^3.0.5",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.12.12",
    "@types/request": "^2.48.1",
    "@types/yargs": "^12.0.1"
  }
}
