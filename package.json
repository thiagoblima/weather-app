{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-app": "node app -a='550 North Maiden Rd. Newnan, GA 30263'",
    "init-project": "rm -rf node_modules && rm -rf environments && rm -rf models && npm install && rm -rf geocode && rm -rf weather && tsc && npm run test-app",
    "test-weather": "node app -a='1301 lambard street philadelphia'",
    "build": "tsc --listEmittedFiles"
  },
  "author": "Thiago Lima",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.13.1",
    "request": "^2.73.0",
    "yargs": "^4.8.1"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/node": "^10.12.12",
    "@types/request": "^2.48.1",
    "@types/yargs": "^12.0.1"
  }
}
